const local: string = 'http://localhost:3000/';
const prod: string = 'https://learndev.herokuapp.com/'

export let domain: string = local;

// be sure that API routes do not match actual URL routes

//-Michael  